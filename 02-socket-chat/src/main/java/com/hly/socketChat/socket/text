                 


next()

输入有效字符之前遇到的空格键、Tab键或Enter键等结束符，next()方法会自动将其过滤掉
读取到有效字符之后，next()方法才将其后的空格键、Tab键或Enter键等视为结束符
next()方法不能得到带空格的字符串。

nextLine()
字面上有扫描一整行的意思
它的结束符只能是Enter键，
返回的是Enter键之前没有被读取的所有字符
可以得到带空格的字符串的。
